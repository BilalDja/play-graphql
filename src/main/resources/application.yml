server:
  port: 8008
spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:testdb
  jpa:
    defer-datasource-initialization: true
